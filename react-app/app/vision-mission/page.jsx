import VisionMission from "@/views/about/VisionMission";
import { pageMetadata } from "@/data/seo";

export const metadata = pageMetadata("/vision-mission");

export default function Page() {
  return <VisionMission />;
}
